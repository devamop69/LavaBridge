{
  "name": "lavalink_proxy",
  "version": "2.0.0",
  "description": "A pure TCP proxy for Lavalink v3 and v4 servers",
  "main": "src/index.js",
  "scripts": {
    "start": "node start.js",
    "start:direct": "node src/index.js",
    "dev": "node start-dev.js",
    "logs": "node -e \"require('fs').readdirSync('./logs').filter(f => f.startsWith('proxy')).sort().slice(-1).forEach(f => console.log(require('fs').readFileSync(`./logs/${f}`, 'utf8')))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lavalink",
    "proxy",
    "tcp",
    "audio"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "express-session": "^1.18.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
